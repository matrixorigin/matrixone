[tool:pytest]
minversion = 6.0
addopts = -ra -q --strict-markers --strict-config
testpaths = .
python_files = test_version_*.py test_matrixone_version_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
    unit: marks tests as unit tests
    version: marks tests related to version management
    standalone: marks tests that don't require external dependencies
asyncio_mode = auto

// Copyright 2021 Matrix Origin
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//      http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

// plus.go is generated by plus.template and overloadGenerate.go, do not edit it directly.

package operator

import (
	"github.com/matrixorigin/matrixone/pkg/container/nulls"
	"github.com/matrixorigin/matrixone/pkg/container/types"
	"github.com/matrixorigin/matrixone/pkg/container/vector"
	"github.com/matrixorigin/matrixone/pkg/encoding"
	"github.com/matrixorigin/matrixone/pkg/vectorize/add"
	"github.com/matrixorigin/matrixone/pkg/vm/process"
)

// FdsOpPlusInt8Int8 :plus operator's evaluation for arguments: [int8,int8]
func FdsOpPlusInt8Int8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int8), rv.Col.([]int8)
	rtl := 1
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt8Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int8AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt8Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int8AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt8Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Int8Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt16Int16 : plus+ operator's evaluation for arguments: [int16,int16]
func FdsOpPlusInt16Int16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int16), rv.Col.([]int16)
	rtl := 2
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt16Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int16AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt16Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int16AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt16Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Int16Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt32Int32 :plus+ operator's evaluation for arguments: [int32,int32]
func FdsOpPlusInt32Int32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int32), rv.Col.([]int32)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int32AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int32AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt32Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Int32Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt64Int64 :plus+ operator's evaluation for arguments: [int64,int64]
func FdsOpPlusInt64Int64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int64), rv.Col.([]int64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Int64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint8Uint8 :plus+ operator's evaluation for arguments: [uint8,uint8]
func FdsOpPlusUint8Uint8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint8), rv.Col.([]uint8)
	rtl := 1
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint8Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint8AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint8Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint8AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint8Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Uint8Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint16Uint16 :plus+ operator's evaluation for arguments: [uint16,uint16]
func FdsOpPlusUint16Uint16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint16), rv.Col.([]uint16)
	rtl := 2
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint16Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint16AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint16Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint16AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint16Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Uint16Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint32Uint32 :plus+ operator's evaluation for arguments: [uint32,uint32]
func FdsOpPlusUint32Uint32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint32), rv.Col.([]uint32)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint32AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint32AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint32Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Uint32Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint64Uint64 :plus+ operator's evaluation for arguments: [uint64,uint64]
func FdsOpPlusUint64Uint64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint64), rv.Col.([]uint64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Uint64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusFloat32Float32 :plus+ operator's evaluation for arguments: [float32,float32]
func FdsOpPlusFloat32Float32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]float32), rv.Col.([]float32)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Float32AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Float32AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeFloat32Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Float32Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusFloat64Float64 :plus+ operator's evaluation for arguments: [float64,float64]
func FdsOpPlusFloat64Float64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]float64), rv.Col.([]float64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Float64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Float64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeFloat64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Float64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusDecimal64Decimal64 :plus+ operator's evaluation for arguments: [decimal64,decimal64]
func FdsOpPlusDecimal64Decimal64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]types.Decimal64), rv.Col.([]types.Decimal64)
	lvScale, rvScale := lv.Typ.Scale, rv.Typ.Scale
	resultScale := lvScale
	if lvScale < rvScale {
		resultScale = rvScale
	}
	resultTyp := types.Type{Oid: types.T_decimal64, Size: 8, Width: 18, Scale: resultScale}
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(resultTyp.Size)*int64(len(rvs)), resultTyp)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeDecimal64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Decimal64AddScalar(lvs[0], rvs, lvScale, rvScale, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(resultTyp.Size)*int64(len(lvs)), resultTyp)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeDecimal64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Decimal64AddScalar(rvs[0], lvs, rvScale, lvScale, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(resultTyp.Size)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeDecimal64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Decimal64Add(lvs, rvs, lv.Typ.Scale, rv.Typ.Scale, rs))
	return vec, nil
}

// FdsOpPlusDecimal128Decimal128 :plus+ operator's evaluation for arguments: [decimal128,decimal128]
func FdsOpPlusDecimal128Decimal128(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]types.Decimal128), rv.Col.([]types.Decimal128)
	lvScale, rvScale := lv.Typ.Scale, rv.Typ.Scale
	resultScale := lvScale
	if lvScale < rvScale {
		resultScale = rvScale
	}
	resultTyp := types.Type{Oid: types.T_decimal128, Size: 16, Width: 38, Scale: resultScale}
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(resultTyp.Size)*int64(len(rvs)), resultTyp)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeDecimal128Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Decimal128AddScalar(lvs[0], rvs, lvScale, rvScale, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(resultTyp.Size)*int64(len(lvs)), resultTyp)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeDecimal128Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Decimal128AddScalar(rvs[0], lvs, rvScale, lvScale, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(resultTyp.Size)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeDecimal128Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Or(lv.Nsp, rv.Nsp, vec.Nsp)
	vector.SetCol(vec, add.Decimal128Add(lvs, rvs, lv.Typ.Scale, rv.Typ.Scale, rs))
	return vec, nil
}

// FdsOpPlusInt8Int16 :plus+ operator's evaluation for arguments: [Int8,Int16]
func FdsOpPlusInt8Int16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int8), rv.Col.([]int16)
	rtl := 2
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt16Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int8Int16AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt16Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = int16(lvs[i])
		}
		vector.SetCol(vec, add.Int16AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt16Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int8Int16Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt8Int32 :plus+ operator's evaluation for arguments: [Int8,Int32]
func FdsOpPlusInt8Int32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int8), rv.Col.([]int32)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int8Int32AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = int32(lvs[i])
		}
		vector.SetCol(vec, add.Int32AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt32Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int8Int32Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt16Int32 :plus+ operator's evaluation for arguments: [Int16,Int32]
func FdsOpPlusInt16Int32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int16), rv.Col.([]int32)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int16Int32AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = int32(lvs[i])
		}
		vector.SetCol(vec, add.Int32AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt32Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int16Int32Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt8Int64 :plus+ operator's evaluation for arguments: [Int8,Int64]
func FdsOpPlusInt8Int64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int8), rv.Col.([]int64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int8Int64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = int64(lvs[i])
		}
		vector.SetCol(vec, add.Int64AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int8Int64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt16Int64 :plus+ operator's evaluation for arguments: [Int16,Int64]
func FdsOpPlusInt16Int64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int16), rv.Col.([]int64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int16Int64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = int64(lvs[i])
		}
		vector.SetCol(vec, add.Int64AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int16Int64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt32Int64 :plus+ operator's evaluation for arguments: [Int32,Int64]
func FdsOpPlusInt32Int64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int32), rv.Col.([]int64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Int32Int64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = int64(lvs[i])
		}
		vector.SetCol(vec, add.Int64AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int32Int64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint8Uint16 :plus+ operator's evaluation for arguments: [Uint8,Uint16]
func FdsOpPlusUint8Uint16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint8), rv.Col.([]uint16)
	rtl := 2
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint16Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint8Uint16AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint16Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = uint16(lvs[i])
		}
		vector.SetCol(vec, add.Uint16AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint16Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint8Uint16Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint8Uint32 :plus+ operator's evaluation for arguments: [Uint8,Uint32]
func FdsOpPlusUint8Uint32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint8), rv.Col.([]uint32)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint8Uint32AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = uint32(lvs[i])
		}
		vector.SetCol(vec, add.Uint32AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint32Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint8Uint32Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint16Uint32 :plus+ operator's evaluation for arguments: [Uint16,Uint32]
func FdsOpPlusUint16Uint32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint16), rv.Col.([]uint32)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint16Uint32AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = uint32(lvs[i])
		}
		vector.SetCol(vec, add.Uint32AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint32Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint16Uint32Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint8Uint64 plus+ operator's evaluation for arguments: [Uint8,Uint64]
func FdsOpPlusUint8Uint64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()

	lvs, rvs := lv.Col.([]uint8), rv.Col.([]uint64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint8Uint64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = uint64(lvs[i])
		}
		vector.SetCol(vec, add.Uint64AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint8Uint64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint16Uint64 :plus+ operator's evaluation for arguments: [Uint16,Uint64]
func FdsOpPlusUint16Uint64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint16), rv.Col.([]uint64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint16Uint64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = uint64(lvs[i])
		}
		vector.SetCol(vec, add.Uint64AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint16Uint64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusUint32Uint64 :plus+ operator's evaluation for arguments: [Uint32,Uint64]
func FdsOpPlusUint32Uint64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint32), rv.Col.([]uint64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Uint32Uint64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = uint64(lvs[i])
		}
		vector.SetCol(vec, add.Uint64AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint32Uint64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusFloat32Float64 :plus+ operator's evaluation for arguments: [Float32,Float64]
func FdsOpPlusFloat32Float64(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]float32), rv.Col.([]float64)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		vector.SetCol(vec, add.Float32Float64AddScalar(lvs[0], rvs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), rv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		for i := range lvs {
			rs[i] = float64(lvs[i])
		}
		vector.SetCol(vec, add.Float64AddScalar(rvs[0], rs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), rv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeFloat64Slice(vec.Data)
	rs = rs[:len(rvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Float32Float64Add(lvs, rvs, rs))
	return vec, nil
}

// FdsOpPlusInt16Int8 :plus+ operator's evaluation for arguments: [Int16,Int8]
func FdsOpPlusInt16Int8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int16), rv.Col.([]int8)
	rtl := 2
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt16Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = int16(rvs[i])
		}
		vector.SetCol(vec, add.Int16AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt16Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int8Int16AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt16Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int8Int16Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusInt32Int8 :plus+ operator's evaluation for arguments: [Int32,Int8]
func FdsOpPlusInt32Int8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int32), rv.Col.([]int8)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = int32(rvs[i])
		}
		vector.SetCol(vec, add.Int32AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int8Int32AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt32Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int8Int32Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusInt32Int16 :plus+ operator's evaluation for arguments: [Int32,Int16]
func FdsOpPlusInt32Int16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int32), rv.Col.([]int16)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = int32(rvs[i])
		}
		vector.SetCol(vec, add.Int32AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int16Int32AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt32Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int16Int32Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusInt64Int8 :plus+ operator's evaluation for arguments: [Int64,Int8]
func FdsOpPlusInt64Int8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int64), rv.Col.([]int8)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = int64(rvs[i])
		}
		vector.SetCol(vec, add.Int64AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int8Int64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt64Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int8Int64Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusInt64Int16 :plus+ operator's evaluation for arguments: [Int64,Int16]
func FdsOpPlusInt64Int16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int64), rv.Col.([]int16)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = int64(rvs[i])
		}
		vector.SetCol(vec, add.Int64AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int16Int64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt64Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int16Int64Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusInt64Int32 :plus+ operator's evaluation for arguments: [Int64,Int32]
func FdsOpPlusInt64Int32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]int64), rv.Col.([]int32)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = int64(rvs[i])
		}
		vector.SetCol(vec, add.Int64AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeInt64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Int32Int64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeInt64Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Int32Int64Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusUint16Uint8 :plus+ operator's evaluation for arguments: [Uint16,Uint8]
func FdsOpPlusUint16Uint8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint16), rv.Col.([]uint8)
	rtl := 2
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint16Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = uint16(rvs[i])
		}
		vector.SetCol(vec, add.Uint16AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint16Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint8Uint16AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint16Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint8Uint16Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusUint32Uint8 :plus+ operator's evaluation for arguments: [Uint32,Uint8]
func FdsOpPlusUint32Uint8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint32), rv.Col.([]uint8)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = uint32(rvs[i])
		}
		vector.SetCol(vec, add.Uint32AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint8Uint32AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint32Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint8Uint32Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusUint32Uint16 :plus+ operator's evaluation for arguments: [Uint32,Uint16]
func FdsOpPlusUint32Uint16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint32), rv.Col.([]uint16)
	rtl := 4
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = uint32(rvs[i])
		}
		vector.SetCol(vec, add.Uint32AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint32Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint16Uint32AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint32Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint16Uint32Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusUint64Uint8 :plus+ operator's evaluation for arguments: [Uint64,Uint8]
func FdsOpPlusUint64Uint8(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint64), rv.Col.([]uint8)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = uint64(rvs[i])
		}
		vector.SetCol(vec, add.Uint64AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint8Uint64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint64Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint8Uint64Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusUint64Uint16 :plus+ operator's evaluation for arguments: [Uint64,Uint16]
func FdsOpPlusUint64Uint16(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint64), rv.Col.([]uint16)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = uint64(rvs[i])
		}
		vector.SetCol(vec, add.Uint64AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint16Uint64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint64Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint16Uint64Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusUint64Uint32 :plus+ operator's evaluation for arguments: [Uint64,Uint32]
func FdsOpPlusUint64Uint32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]uint64), rv.Col.([]uint32)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = uint64(rvs[i])
		}
		vector.SetCol(vec, add.Uint64AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeUint64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Uint32Uint64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeUint64Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Uint32Uint64Add(rvs, lvs, rs))
	return vec, nil
}

// FdsOpPlusFloat64Float32 :plus+ operator's evaluation for arguments: [Float64,Float32]
func FdsOpPlusFloat64Float32(vs []*vector.Vector, proc *process.Process) (*vector.Vector, error) {
	lv := vs[0]
	rv := vs[1]
	lc := lv.IsConstant()
	rc := rv.IsConstant()
	lvs, rvs := lv.Col.([]float64), rv.Col.([]float32)
	rtl := 8
	switch {
	case lc && !rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(rvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat64Slice(vec.Data)
		rs = rs[:len(rvs)]
		nulls.Set(vec.Nsp, rv.Nsp)
		for i := range rvs {
			rs[i] = float64(rvs[i])
		}
		vector.SetCol(vec, add.Float64AddScalar(lvs[0], rs, rs))
		return vec, nil
	case !lc && rc:
		vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
		if err != nil {
			return nil, err
		}
		rs := encoding.DecodeFloat64Slice(vec.Data)
		rs = rs[:len(lvs)]
		nulls.Set(vec.Nsp, lv.Nsp)
		vector.SetCol(vec, add.Float32Float64AddScalar(rvs[0], lvs, rs))
		return vec, nil
	}
	vec, err := process.Get(proc, int64(rtl)*int64(len(lvs)), lv.Typ)
	if err != nil {
		return nil, err
	}
	rs := encoding.DecodeFloat64Slice(vec.Data)
	rs = rs[:len(lvs)]
	nulls.Set(vec.Nsp, rv.Nsp.Or(lv.Nsp))
	vector.SetCol(vec, add.Float32Float64Add(rvs, lvs, rs))
	return vec, nil
}
